<%# <%= link_to "dungeons", dungeons_path, class: "btn-btn-flat" %>
<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <h3>Make your booking</h3>
        <%= simple_form_for [@dungeon, @booking] do |booking| %>
          <%= booking.input :dates, placeholder: 1 %>
          <%= booking.submit class: 'btn btn-flat' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
